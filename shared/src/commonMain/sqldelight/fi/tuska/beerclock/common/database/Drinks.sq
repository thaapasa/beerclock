CREATE TABLE drinks (
  id INTEGER PRIMARY KEY NOT NULL,
  time INTEGER NOT NULL,
  drink TEXT NOT NULL
);

CREATE INDEX drinks_time ON drinks(time);

selectAll:
SELECT *
FROM drinks;

insert:
INSERT INTO drinks(time, drink)
VALUES (?, ?);
